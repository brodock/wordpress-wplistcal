- Make it so {text %START% word} only prints 'text' and 'word' if %START% is not empty
- All day events
- Change internal representation of dates to support dates > 2038
	- Probably use something like D203809121704 (DYYYYMMDDHHMM) using the D to switch parsers accordingly
	- Will need to ensure that converting the column from BIGINT to VARCHAR(13) is safe, otherwise, use a converter in upgrade